# Modèle de secret — COPIE en .env puis mets ta vraie clé.
#   cp .env.example .env
# Le vrai .env n'est JAMAIS versionné (voir .gitignore).

# Clé Groq (https://console.groq.com/keys)
GROQ_API_KEY=gsk_remplace_par_ta_cle
